# Notas
Primer programa node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Da la base para imprimir la tabla de multiplicar       [number]
  -l, --listar   Muestra la tabla en consola        [boolean] [default: "false"]
  -h, --hasta    Limite de nuestra tabla de multiplicar   [number] [default: 10]
```